# V0.2 LangGraph Native Runtime + Checkpointer + Subagent — Implementation Plan

**Spec:** [`docs/superpowers/specs/2026-05-29-v0.2-langgraph-checkpointer-subagent-design.md`](../specs/2026-05-29-v0.2-langgraph-checkpointer-subagent-design.md)

**Goal:** Replace the V0.1 hand-rolled loop with a real LangGraph graph backed by a pluggable checkpointer, land Subagent Runtime, and expose a streaming API.

**Tech:** Latest `langgraph` (1.2.x at time of writing), `langgraph-checkpoint-sqlite>=2`, `langgraph-checkpoint-postgres` (lazy), `langchain>=1.0`, existing stack. **Hard rule: never pin langgraph to an older line — always ride latest.**

**Test discipline:** TDD per task. Each task = write failing test → minimal impl → green → commit. Push at end of each milestone.

---

## File map

**New packages**

- `src/modi_harness/checkpoint/` — `factory.py`, `errors.py`, `__init__.py`
- `src/modi_harness/graph/` — `state.py`, `builder.py`, `nodes.py`, `trace_middleware.py`, `__init__.py`
- `src/modi_harness/subagent/` — `dispatcher.py`, `__init__.py`

**New tests**

- `tests/checkpoint/test_factory.py`
- `tests/graph/test_state.py`, `test_builder.py`, `test_nodes.py`, `test_trace_middleware.py`
- `tests/subagent/test_dispatcher.py`, `test_e2e.py`
- `tests/runtime/test_cross_process_resume.py`
- `tests/streaming/test_stream.py`

**Files modified**

- `pyproject.toml`
- `src/modi_harness/types.py` (reducers, ToolSpec.kind, PermissionProfile fields)
- `src/modi_harness/config/settings.py` (CheckpointSettings, SubagentSettings)
- `src/modi_harness/runtime/adapter.py` (rewrite: thin wrapper over graph)
- `src/modi_harness/api/harness.py` (thread_id keyed; add stream/resume)
- `src/modi_harness/tools/registry.py` (defaults for kind/subagent_target)
- `src/modi_harness/tools/gateway.py` (subagent branch)
- `src/modi_harness/policy/gate.py` (subagent rules)
- `src/modi_harness/workspace/manager.py` (drop snapshot, add create_child_run)
- `src/modi_harness/agents/loader.py` (parse new YAML keys)
- `src/modi_harness/models/adapter.py` (astream)
- `src/modi_harness/__main__.py` (CLI keyed by thread_id)

---

## N0 — Checkpoint Infrastructure & Type Refactor

- [x] **N0.1** Bump to latest langgraph 1.x stack (`langgraph>=1.0`, `langgraph-checkpoint-sqlite>=2`, `langchain>=1.0`, `langchain-openai>=1.0`); `uv sync --extra dev`; sanity-import `Command`/`interrupt`/`SqliteSaver`/`MemorySaver`; full suite still 210 green; commit.
- [x] **N0.2** Extend `AgentState` with `Annotated[..., operator.add]` reducers on `messages`, `tool_calls`, `denied_actions`, `workspace_refs`, `pending_trace_events`; add fields `parent_thread_id`, `repair_used`, `pending_trace_events`. TDD via `tests/graph/test_state.py`.
- [x] **N0.3** Add `kind` and `subagent_target` to `ToolSpec`; default `"regular"`/`None` in `ToolRegistry`. Extend `tests/tools/test_registry.py`.
- [x] **N0.4** Add `allowed_subagents` and `subagent_max_depth` to `PermissionProfile`; parse from agent YAML frontmatter. Extend `tests/agents/test_loader.py`.
- [x] **N0.5** Add `CheckpointSettings` (backend/sqlite_path/postgres_dsn) and `SubagentSettings` (max_depth) groups; flat-env mappings `MODI_CHECKPOINT_*`, `MODI_SUBAGENT_MAX_DEPTH`. Extend `tests/config/test_settings.py`.
- [x] **N0.6** Create `modi_harness/checkpoint/` with `build_checkpointer(settings) -> BaseCheckpointSaver` dispatching memory / sqlite / postgres (postgres lazy-imported). `tests/checkpoint/test_factory.py` covers all three plus the missing-DSN error path.
- [x] **N0.7** Full suite green; push.

**Exit gate:** `build_checkpointer` works on memory + sqlite; types carry V0.2 fields; existing 210 still green.

---

## N1 — LangGraph Main Graph

- [x] **N1.1** Create `graph/state.py`: re-export `AgentState` as `MainGraphState` (or alias). Sanity test asserting it's the same TypedDict instance the rest of the codebase uses.
- [x] **N1.2** Create `graph/nodes.py`: extract the 8 step functions out of the V0.1 `_loop` — `load_agent`, `load_skills`, `build_context`, `model_step`, `route_response`, `execute_tool`, `validate_output`, `persist_state`. Each takes `(state, config) -> dict` (partial state). No IO inside nodes; they append `TraceEvent`s to `state["pending_trace_events"]` and call governance modules as pure functions. Deps (loaders, gateway, policy, etc.) are pulled from `config["configurable"]["modi_deps"]`. Unit tests per node under `tests/graph/test_nodes.py`.
- [x] **N1.3** Create `graph/trace_middleware.py`: a per-thread, per-process cursor that drains `state["pending_trace_events"]` to `TraceRecorder` between node transitions. On resume in a fresh process, rebuild cursor by reading existing `trace.jsonl` and skipping events whose `event_id` is already present. Tests: cursor advances; duplicate `event_id` suppressed; idempotent under crash/resume.
- [x] **N1.4** Create `graph/builder.py`: `build_main_graph(deps, checkpointer) -> CompiledGraph`. Wires the 4 nodes (setup/model_turn/execute_tool/validate_output) per the spec, attaches the checkpointer.
- [x] **N1.5** Implement `interrupt()` in `execute_tool`: when Policy Gate returns `require_approval`/`require_review`, raise `langgraph.types.Interrupt(payload=PendingApproval-shaped dict)`. Unit test confirms the graph pauses and `graph.get_state` shows `pending_approval` set.
- [x] **N1.6** Rewrite `runtime/adapter.py`: remove `_loop`, `_RunContext`, `_runs`, `approve`, `reject`. New `RuntimeAdapter` holds the compiled graph and exposes `run(input) -> RunTaskResponse`, `resume(thread_id, payload) -> RunTaskResponse`. `run` calls `graph.invoke` with `config={"configurable": {"thread_id": ..., "modi_deps": ...}}`; `resume` uses `Command(resume=payload)`. Rewrite `tests/runtime/test_loop.py` against the new shape (6 cases preserved semantically).
- [x] **N1.7** Rewrite `api/harness.py`: drop `start_thread`; key all introspection (`get_state`, `get_artifacts`, `get_trace`, `get_denials`) by `thread_id`; add `resume_task(thread_id, payload)`; `approve_action` / `reject_action` become thin wrappers over `resume_task` building `Command(resume={"approval_id": ..., "decision": ...})`. Rewrite `tests/test_harness.py`.
- [x] **N1.8** Strip `WorkspaceManager.save_state` and `snapshot_state`; keep `index_workspace`, `create_run`, `append_log`. Drop the snapshot tests in `tests/workspace/test_manager.py`.
- [x] **N1.9** Update `__main__.py` CLI: `modi run` now treats `--thread-id` as primary; `modi resume --thread-id X` added.
- [x] **N1.10** Full suite green; push.

**Exit gate:** Every previously passing V0.1 test now drives the LangGraph runtime via the rewritten facade. `_runs` dict is gone from the codebase (`grep` confirms). `interrupt()` + `Command(resume=)` round-trip works in-process.

---

## N2 — Subagent Runtime

- [x] **N2.1** Implement `subagent/dispatcher.py`: `dispatch_subagent(...)`. Steps per spec §6: schema-validate child input, visibility check against parent `allowed_subagents`, denied-retry guard, parent `pre_tool_use` hooks, Policy Gate subagent decision (depth + permission_mode tightening), build child state with propagated `parent_thread_id`/`parent_run_id`/`root_run_id`/`denied_actions`, child `thread_id = f"{parent_thread_id}/sub/{ulid}"`, invoke child compiled graph, handle child status (`completed` / `interrupted` / `failed` / `blocked`), propagate child `denied_actions` diff and `workspace_refs` back to parent, run parent `post_tool_use` hooks, emit trace event with `child_thread_id` (no inline child trace). Unit tests in `tests/subagent/test_dispatcher.py`.
- [x] **N2.2** Auto-register subagent tools at `ModiHarness.__init__`: for each agent discovered by `AgentLoader.list_agent_names()`, register `delegate_to_<name>` ToolSpec with `kind="subagent"`, `subagent_target=<name>`, `risk_level="L2"`, `side_effect=True`, input schema `{task, permission_mode?, rationale}`. Test: harness instantiated with N agent dirs has N matching `delegate_to_*` entries in the registry.
- [x] **N2.3** Wire the subagent branch into `tools/gateway.py`: when `spec["kind"] == "subagent"`, route to `dispatch_subagent` instead of the registered handler; preserve the existing `ToolDispatchResult` contract so `execute_tool` node is agnostic.
- [x] **N2.4** Extend `policy/gate.py`/dispatcher with two new rules: (a) depth check — deny if `parent_thread_id.count("/sub/") + 1 > effective_max_depth`; (b) permission-mode tightening — deny if requested child mode is laxer than parent (strictness order `bypass < auto < ask < plan`). Effective max depth = `agent.permission_profile.subagent_max_depth or settings.subagent.max_depth`.
- [x] **N2.5** Add `WorkspaceManager.create_child_run(parent_run_id, child_run_id)`: lays out `.modi/workspace/<root>/sub/<child>/{input,artifacts,drafts,logs}`.
- [x] **N2.6** Context Manager filters `delegate_to_*` tools by `agent.permission_profile.allowed_subagents` before exposing to the model (`[]` → none, `["*"]` → all, list → intersect).
- [x] **N2.7** Subagent trust boundary: child output wrapped as `ContextBlock(trust="untrusted", source_kind="subagent_result")` before the parent's `OutputController` sees it.
- [x] **N2.8** Implement 9 e2e scenarios in `tests/subagent/test_e2e.py`: happy-path, allowed_subagents=[], laxer-mode denied, depth limit, parent denied propagated to child, child output untrusted, all-agents-have-delegate-tools, unknown-target, allowed_subagents-context-filter.
- [x] **N2.9** Full suite green; push.

**Exit gate:** All 9 subagent e2e tests pass with the canned scripted model. Denied-action propagation bi-directional. Depth + mode-tightening enforced by Policy Gate, not by ad-hoc checks in the dispatcher.

---

## N3 — Streaming

- [x] **N3.1** Sync `RuntimeAdapter.stream()` wraps `graph.stream(stream_mode="updates")` and projects per-node updates into Modi `StreamEvent`-shaped dicts.
- [x] **N3.2** Add `ModiHarness.stream(*, agent, input, thread_id=None, permission_mode=None, options=None)` — yields events including `model_delta`, `tool_call_proposal`, `tool_call_result`, `approval_request`, `terminal` (carrying full `RunTaskResponse`).
- [x] **N3.3** Streaming smoke tests in `tests/streaming/test_stream.py`: terminal-only happy path, tool-call sequence, and streaming-vs-non-streaming equivalence on terminal payload.
- [x] **N3.4** Full suite green; push.

**Exit gate:** `stream()` returns a normalized iterator; smoke run with the scripted model produces the canonical event order; terminal event matches non-streaming response.

---

## N4 — Cross-Process Resume + V0.1 Test Rewrite

- [x] **N4.1** V0.1 smoke scenarios S1–S6 rewritten under V0.2 semantics (already updated during N1 to use `thread_id`).
- [x] **N4.2** Add S7 cross-process resume in `tests/runtime/test_cross_process_resume.py`: Process A runs to interrupt and exits; Process B (spawned via `subprocess.run`) opens the same sqlite checkpointer and calls `approve_action(thread_id, approval_id)`. Asserts completed status.
- [x] **N4.3** Add S8 subagent denied-flow smoke in `tests/test_smoke_scenarios.py` — denied action survives across delegate_to_research dispatch.
- [x] **N4.4** `uv run pytest -m smoke -q`: S1–S8 all pass (8 total).
- [x] **N4.5** Full suite green (247 tests).
- [x] **N4.6** Push.

**Exit gate:** Resume across processes proven end-to-end. All historical V0.1 behavior preserved under V0.2 wiring. `pytest -m smoke` covers S1–S8.

---

## N5 — Documentation + Release

- [x] **N5.1** Update `docs/architecture/04-runtime-adapter.md` to LangGraph-first: graph topology, node contracts, interrupt/resume, checkpointer integration. Drop references to the hand-rolled state machine.
- [x] **N5.2** Move `docs/architecture/future/subagent-runtime.md` → `docs/architecture/16-subagent-runtime.md`; rewrite against the shipped design (dispatcher, trust boundary, denied propagation, depth, allowed_subagents).
- [x] **N5.3** Write `docs/architecture/17-checkpointer.md`: backends, settings, single-host sqlite caveat, multi-host postgres guidance, resume semantics, trace reconciliation.
- [x] **N5.4** Update `docs/types-reference.md`:
  - AgentState reducer notes + the three new fields.
  - ToolSpec.kind / subagent_target.
  - PermissionProfile.allowed_subagents / subagent_max_depth.
  - StreamEvent v0.2 event_types and projection rules.
- [x] **N5.5** Update `docs/development-plan.md`: mark V0.1 complete; add V0.2 milestones with status; preview V0.3 placeholders.
- [x] **N5.6** Update `docs/table_of_contents.md` for the new architecture docs and any moved files.
- [x] **N5.7** Append `CHANGELOG.md` V0.2 entry: breaking changes, new features, deprecations.
- [x] **N5.8** Append `docs/changelog.md` document-scope entry: docs moves (future/ → numbered), new chapters (16/17), spec → shipped status flip.
- [x] **N5.9** Mark the V0.2 spec file `Status: shipped` and date.
- [x] **N5.10** Final green: `uv sync && uv run pytest`.
- [x] **N5.11** Tag and push: `git tag v0.2.0 && git push origin main v0.2.0`.

**Exit gate (V0.2 release):**
1. `uv sync && uv run pytest` green.
2. `uv run pytest -m smoke` covers S1–S8 green.
3. `modi run --agent X --task Y --thread-id T` works end-to-end against LangGraph.
4. Interrupt in one process, resume in another via sqlite file — proven by S7.
5. Sample agent with `allowed_subagents` configured can delegate; depth + mode + denied propagation enforced.
6. V0.2 spec file marked `Status: shipped`.

---

## Risks watch (during execution)

- **LangGraph API surface drift inside 0.2.x** — if minor releases shift `interrupt`/`Command` semantics, pin tighter and absorb in `graph/builder.py`.
- **Sqlite multi-process contention** — checkpointer opens with `check_same_thread=False`; WAL mode enabled by SqliteSaver. If S7 flakes on macOS, switch to file-lock around `invoke`.
- **Trace duplication on resume** — middleware MUST dedupe by `event_id` against on-disk `trace.jsonl` before writing. N1.3 covers this; revisit if S7 shows duplicates.
- **State bloat from append-only reducers** — accepted for V0.2; V0.3 may add periodic compaction.

---

## Self-review checklist (run after N5)

- [x] Every spec section under §Detailed Design has a corresponding task. (§1 graph → N1.4; §2 state → N0.2; §3 interrupt → N1.5; §4 checkpointer → N0.6; §5 thread_id naming → N2.1; §6 subagent → N2.1–N2.7; §7 trace → N1.3; §8 API → N1.7; §9 streaming → N3.2; §10 subagent display → N2.6.)
- [x] All 9 subagent matrix tests + 2 streaming + S1–S8 smokes accounted for.
- [x] No leftover references to `_runs`, `_RunContext`, `save_state`, `snapshot_state`, `start_thread` in source.
- [x] V0.2 spec status flipped to `shipped`.
- [x] CHANGELOG and docs/changelog both updated.

---

## Execution

Default execution: **subagent-driven** (one fresh subagent per task, review between tasks). Switch to **inline batched** for N0 and N5 if the user prefers fewer hand-offs on short tasks.
